
pub enum Test
{
    Yes,
    No,
}











mod tests
{
    use inkwell::types::BasicType;


    fn a_test(ctx: inkwell::context::Context)
    {
        let field_types: Vec<BasicType> = vec![];
        field_types.push(c1241251);
        ctx.struct_type(field_types, packed);
    }

}
